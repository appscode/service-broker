apiVersion: servicecatalog.k8s.io/v1beta1
kind: ServiceInstance
metadata:
  name: my-broker-mongodb-instance
  namespace: db-broker
  labels:
    app: db-broker
spec:
  clusterServiceClassExternalName: mongo
  clusterServicePlanExternalName: default
