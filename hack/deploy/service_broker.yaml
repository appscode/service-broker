apiVersion: servicecatalog.k8s.io/v1beta1
kind: ServiceBroker
metadata:
  name: ${SERVICE_BROKER_NAME}
  namespace: ${SERVICE_BROKER_NAMESPACE}
  labels:
    app: service-broker
spec:
  url: http://${SERVICE_BROKER_NAME}.${SERVICE_BROKER_NAMESPACE}.svc.cluster.local
